# フロントエンドページテストの業務ロジック対応表

このドキュメントは、`src/pages/*.test.tsx` が何を業務的に保証しているかを把握するための一覧です。
実装詳細ではなく、画面として守るべき振る舞いを基準に整理しています。

## テスト運用コマンド（Frontend React）
- Frontend React向けテストスキル: `npm run test:skill:frontend:react`
- 互換エイリアス: `npm run test:skill` / `npm run test:skill:react`
- グローバルコマンド: `frontend-react-test-skill`
- 他プロジェクトを対象に実行する場合:
  - `node ./scripts/check-test-skill.mjs --root <対象ディレクトリ>`
  - 例: `node ./scripts/check-test-skill.mjs --root C:/path/to/another-frontend`

## AccountCreate
- 対象: `src/pages/AccountCreate.test.tsx`
- 業務上の保証:
  - 管理者が必要項目を正しく入力すれば、アカウントを新規作成できる。
  - パスワード確認が不一致の場合は作成を止め、ユーザーにエラーを伝える。
- 主な確認シナリオ:
  - 正常系: 入力完了後に作成成功メッセージと作成済みアカウントが表示される。
  - 異常系: 確認用パスワード不一致でバリデーションエラーが表示される。

## Home
- 対象: `src/pages/Home.test.tsx`
- 業務上の保証:
  - ダッシュボードはAPI結果に応じてタスク集計と最近更新企業を表示できる。
  - データ未登録時は空状態メッセージで次アクションを案内できる。
- 主な確認シナリオ:
  - タスク・企業データありで一覧と導線リンクが表示される。
  - タスク・企業データなしで空状態メッセージが表示される。

## NotFound
- 対象: `src/pages/NotFound.test.tsx`
- 業務上の保証:
  - 存在しないURLにアクセスしたユーザーを迷子にしない。
  - 主要画面への復帰導線が機能する。
- 主な確認シナリオ:
  - 404メッセージと主要リンクを表示する。
  - 「ダッシュボードへ戻る」でトップへ遷移できる。

## WholesaleDetail
- 対象: `src/pages/WholesaleDetail.test.tsx`
- 業務上の保証:
  - 卸の詳細情報と関連タスクを正しく参照できる。
  - 編集保存が反映され、最新状態を確認できる。
  - 削除フロー完了後に業務上の戻り先へ遷移できる。
- 主な確認シナリオ:
  - 詳細表示: 卸情報、ステータス、関連タスクの表示。
  - 更新: 編集後に保存し、更新成功通知と更新後ステータス表示。
  - 削除: 確認ダイアログで削除実行後に案件詳細へ遷移。

## Projects
- 対象: `src/pages/Projects.test.tsx`
- 業務上の保証:
  - 案件一覧画面で案件を取得・表示できる。
  - 検索条件に応じた再取得で一覧が更新される。
  - 案件作成フォームの必須入力（企業・案件名）が守られる。
  - 案件作成成功後に通知と一覧反映が行われる。
  - 担当者変更時に更新結果が一覧へ反映される。
  - 閲覧専用ユーザーには作成・担当者変更操作を表示しない。
- 主な確認シナリオ:
  - 初期表示で案件名・企業名が表示される。
  - 検索入力で検索クエリ付きAPIが呼ばれ、結果が切り替わる。
  - 必須未入力で作成エラー表示し、作成API未実行。
  - 案件作成成功時の成功通知と作成案件表示。
  - 担当者変更後の成功通知と選択値更新。
  - 読み取り専用ユーザー向けの操作制御。

## Companies
- 対象: `src/pages/Companies.test.tsx`
- 業務上の保証:
  - 企業一覧の取得表示ができる。
  - フィルタ操作で検索条件が反映される。
  - 企業作成後の外部連携失敗時にも、ユーザーに適切な情報を返せる。
- 主な確認シナリオ:
  - 企業一覧の表示。
  - フィルタ変更時の再検索。
  - 企業作成成功かつChatwork連携失敗時の情報トースト表示。

## CompanyDetail
- 対象: `src/pages/CompanyDetail.test.tsx`
- 業務上の保証:
  - 連絡先入力の必須バリデーションが効く。
  - 検索条件入力により検索API連携が行われる。
  - メッセージへのラベル付与操作が反映される。
  - メッセージラベル削除操作が反映される。
  - 重複連絡先の統合で重複データを整理できる。
  - 連絡先並び替え失敗時に順序をロールバックしエラーを提示できる。
  - 案件/卸/サマリーの各タブで関連データを参照できる。
- 主な確認シナリオ:
  - 空の連絡先名でバリデーションエラー。
  - 検索語入力時に検索API呼び出し。
  - ラベル追加操作でラベル付与API呼び出し。
  - ラベル削除操作で表示からラベルが消える。
  - 重複統合確認ダイアログ経由で重複連絡先を統合。
  - 並び替え失敗時のエラー表示と順序復元。
  - タブ切り替え時の案件/卸/サマリー表示確認。

## Tasks
- 対象: `src/pages/Tasks.test.tsx`
- 業務上の保証:
  - タスク一覧の取得と表示ができる。
  - 管理者の表示範囲切り替え時に取得対象が切り替わる。
  - ターゲット種別などの絞り込み条件が検索に反映される。
  - 作成フォームの必須項目チェック（タイトル・企業選択）が機能する。
  - 一般タスクの新規作成後、一覧へ反映される。
  - ステータス更新失敗時に表示をロールバックしエラーを提示する。
  - 一括更新で更新項目未指定時は実行を止める。
  - 一括更新後にステータスが反映され、選択状態がクリアされる。
  - 削除確認を経て、対象タスクを安全に削除できる。
  - 書き込み権限がない利用者には更新操作を表示しない。
- 主な確認シナリオ:
  - タスク一覧表示。
  - 管理者の表示範囲切り替えで全件API再取得。
  - フィルタ適用時のクエリ反映。
  - 必須未入力での作成エラー表示。
  - 一般タスク作成成功と一覧反映。
  - ステータス更新失敗時のエラー表示と表示復元。
  - 一括更新の更新項目未指定エラー（API未実行）。
  - 一括更新成功時の結果反映と選択解除。
  - 削除確認ダイアログ経由での削除完了。
  - 読み取り専用ユーザー向け操作制御。

## TaskDetail
- 対象: `src/pages/TaskDetail.test.tsx`
- 業務上の保証:
  - 権限に応じて編集可否が制御される。
  - タイトル必須バリデーションと更新処理が機能する。
- 主な確認シナリオ:
  - 書き込み権限なしユーザーへの読み取り専用案内。
  - 空タイトル入力時エラー、正常入力時更新実行。

## ProjectDetail
- 対象: `src/pages/ProjectDetail.test.tsx`
- 業務上の保証:
  - 案件詳細と関連卸一覧を取得して正しく表示できる。
  - 案件編集を保存すると最新内容が画面へ反映される。
  - 案件名必須バリデーションで空保存を防止する。
  - 卸追加時、企業未選択なら登録を止めてエラーを提示できる。
  - 卸編集モーダルで保存した内容が一覧へ反映される。
  - 卸削除失敗時にエラー表示し、対象データを保持する。
  - 卸削除確認後に対象データを除去し、空状態を案内できる。
  - 閲覧専用ユーザーには卸の更新操作を表示しない。
- 主な確認シナリオ:
  - 案件詳細・卸一覧の表示。
  - 案件編集保存後の表示更新。
  - 案件名未入力での保存エラー（更新API未実行）。
  - 企業未選択時の卸作成バリデーション。
  - 卸編集保存後のステータス更新表示。
  - 卸削除失敗時のエラー表示とデータ保持。
  - 削除確認後の一覧更新と空状態表示。
  - 読み取り専用ユーザー向け操作制御。

## Feedback
- 対象: `src/pages/Feedback.test.tsx`
- 業務上の保証:
  - フィードバック送信前の必須入力バリデーションが効く。
  - 余計な空白を除去して送信できる。
  - 既存の改善項目を編集して保存できる。
- 主な確認シナリオ:
  - メッセージ未入力時のエラー表示。
  - trim済みデータで送信。
  - 改善項目の更新保存。

## Login
- 対象: `src/pages/Login.test.tsx`
- 業務上の保証:
  - パスワード表示切り替えが機能し、入力支援を提供できる。
  - 入力中に認証エラー表示を解消し、再試行しやすい状態を保てる。
- 主な確認シナリオ:
  - パスワード表示/非表示の切り替え。
  - 入力変更時の認証エラークリア。

## ChatworkSettings
- 対象: `src/pages/ChatworkSettings.test.tsx`
- 業務上の保証:
  - 権限不足時は操作不可であることを明示できる。
  - ルーム同期ジョブ完了時に完了通知と最新ルーム一覧反映ができる。
  - メッセージ同期開始時にジョブ種別を明示できる。
  - ルーム単位操作と一括有効化操作が期待どおりに機能する。
  - 一括更新失敗時にエラー表示し、選択状態を維持できる。
  - 検索・状態フィルタ・ページネーションを組み合わせて絞り込める。
  - エラー状態フィルタで障害ルームを抽出できる。
  - ジョブキャンセル時に状態と通知が整合して表示される。
- 主な確認シナリオ:
  - 権限不足メッセージ表示。
  - ルーム同期実行後の完了通知と再取得結果表示。
  - メッセージ同期実行後のジョブ種別表示。
  - 選択ルームに対する一括有効化。
  - 一括有効化失敗時のエラー表示と選択状態維持。
  - 検索・状態フィルタ適用とページ送り操作。
  - エラー状態フィルタによる障害ルーム抽出。
  - キャンセル操作後の状態・通知表示。

## 追加したコンポーネント/Hookテスト（今回）
- 対象: `src/hooks/useFilterForm.test.tsx`
- 業務上の保証:
  - フィルター操作時のページング制御（リセット有無）が仕様どおりである。
  - `clear` / `submit` / `active判定` などのフォーム制御が破綻しない。
- 主な確認シナリオ:
  - 単一更新・複数更新・クリア・送信時の状態更新。
  - `resetPageOnFilterChange=false` 時のページ維持。

- 対象: `src/hooks/useFormState.test.tsx`
- 業務上の保証:
  - 汎用フォーム状態の更新・初期化・差し替えが正しく機能する。
  - フォームメッセージ（成功/失敗）のクリアが期待どおりに機能する。
- 主な確認シナリオ:
  - `setField` / `setFields` / `reset` / `resetWith`。
  - `clearError` / `clearSuccess` / `clearMessages`。

- 対象: `src/components/Layout.test.tsx`
- 業務上の保証:
  - サイドバー開閉状態の永続化（`localStorage`）が正しく機能する。
  - ロールに応じたナビ項目の表示制御が機能する。
- 主な確認シナリオ:
  - 開閉状態の復元と再保存。
  - `viewer` で設定メニュー非表示、`admin` で表示。

- 対象: `src/components/companies/CompanyProjectsTab.test.tsx`
- 業務上の保証:
  - 企業詳細の案件タブで、読み込み/空/エラー/通常表示が壊れない。
  - 期間・単価・更新日の表示フォーマットが維持される。

- 対象: `src/components/companies/CompanyWholesalesTab.test.tsx`
- 業務上の保証:
  - 企業詳細の卸タブで、読み込み/空/エラー/通常表示が壊れない。
  - 案件名フォールバック・単価/マージン/成立日の表示整合が維持される。

- 対象: `src/components/companies/CompanySummariesTab.test.tsx`
- 業務上の保証:
  - 企業詳細のサマリータブで、読み込み/空/エラー/通常表示が壊れない。
  - 期間・作成日・種別・参照件数の表示仕様を維持できる。

- 対象: `src/components/companies/CompanyTasksSection.test.tsx`
- 業務上の保証:
  - 企業詳細のタスク追加/絞り込み/更新/権限制御が機能する。
  - タイトル必須や更新失敗時のUI整合が崩れない。

- 対象: `src/components/companies/CompanyOverviewTab.test.tsx`
- 業務上の保証:
  - 企業概要タブの表示モード/編集モード双方の主要操作が壊れない。
  - カテゴリ・ステータス・担当者・タグ・プロフィール編集の連携が維持される。

## テスト運用スクリプト（.mjs）
- 対象: `scripts/check-test-skill.mjs`
- 目的:
  - SKILL準拠ルール（日本語テスト名、`fireEvent`禁止、`contexts/hooks`の直接モック禁止）を自動検査する。
  - テストコード品質の最低ラインを機械的に担保する。

- 対象: `scripts/check-coverage-targets.mjs`
- 目的:
  - グローバル閾値と重点ファイル別閾値を `coverage/coverage-final.json` で検査する。
  - 「不足テストが再発してもCIで止める」ためのガードとして機能させる。
